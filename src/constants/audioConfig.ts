import type { AudioConfig } from "../project/projectTypes"

export const DEFAULT_AUDIO_CONFIG: AudioConfig = {
  volume: 1.0,
  muted: false,
  fadeInDuration: 0,
  fadeOutDuration: 0,
  balance: 0,
}
